import Login from "@/pages/login";

const Home = () => {
  return (
    <div className="h-[100dvh] w-screen flex items-center justify-center">
      <Login />
    </div>
  );
};

export default Home;
